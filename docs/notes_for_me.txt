These are simply the commands to build the .py files into an executable, because I can never remember them, and there's no good single source online that has the right ones.

OS X:
'pyinstaller --onefile --windowed --icon=icon.icns KA-Randomize.py'

Windows:
'pyinstaller.exe --onefile --windowed --icon=icon.ico KA-Randomize.py
